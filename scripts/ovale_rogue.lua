local OVALE, Ovale = ...
local OvaleScripts = Ovale.OvaleScripts

do
	local name = "example_rogue"
	local desc = "Example Rogue"
	local code = [[
# Example rogue script.
]]
	--OvaleScripts:RegisterScript("ROGUE", nil, name, desc, code, "script")
end
