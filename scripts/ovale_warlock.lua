local OVALE, Ovale = ...
local OvaleScripts = Ovale.OvaleScripts

do
	local name = "example_warlock"
	local desc = "Example Warlock"
	local code = [[
# Example warlock script.
]]
	--OvaleScripts:RegisterScript("WARLOCK", nil, name, desc, code, "script")
end
