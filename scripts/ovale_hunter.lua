local OVALE, Ovale = ...
local OvaleScripts = Ovale.OvaleScripts

do
	local name = "example_hunter"
	local desc = "Example Hunter"
	local code = [[
# Example hunter script.
]]
	--OvaleScripts:RegisterScript("HUNTER", nil, name, desc, code, "script")
end
